Installation on Debian operating system.
============ == ====== ========= ======

BEFORE INSTALL:

$ sudo apt install mesa-common-dev libglu1-mesa-dev freeglut3-dev
$ sudo apt install python3 pip3 python3-pygame
$ sudo pip3 install kivymd
